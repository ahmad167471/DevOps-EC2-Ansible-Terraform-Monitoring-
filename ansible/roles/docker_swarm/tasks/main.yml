- name: Install Docker
  apt:
    name: docker.io
    update_cache: true

- name: Start Docker
  service:
    name: docker
    state: started
    enabled: true

- name: Initialize Docker Swarm
  command: docker swarm init
  ignore_errors: true